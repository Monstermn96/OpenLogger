version: 1
applications:
  - appRoot: .
    backend:
      phases:
        preBuild:
          commands:
            - echo "Checking CDK Bootstrap..."
        build:
          commands:
            - npm install
            - CDK_DEFAULT_ACCOUNT=630658076165 CDK_DEFAULT_REGION=us-east-1 npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID --outputs-out-dir .
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm --prefix apps/web run build
      artifacts:
        baseDirectory: apps/web/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - apps/web/node_modules/**/*

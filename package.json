{
  "name": "openlogger",
  "version": "1.0.0",
  "private": true,
  "description": "OBD2 Data Logger and Car Diagnostics Web App",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "start": "npm run start --workspace=apps/web"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
